

#include <stdio.h>

int main()
{
    int a, b, c;

    scanf("%d, %d, %d", &a, &b, &c:);

    if (a>b) {
        if(a>c)
            printf("max = %d", a);/* code */
        else
            printf("max = %d", c);/* code */

    }

    if(a<b){
        if(b>c)
            printf("max = %d", b);
        else
            printf("max = %d", c:);
    }

    return 0;
}
